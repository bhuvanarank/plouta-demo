import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  // ... existing code ...
  subHeading: {
    fontSize: 16,
    color: '#666',
    textAlign: 'center',
    marginBottom: 24,
  },
  resendContainer: {
    marginTop: 24,
    alignItems: 'center',
  },
  timerText: {
    fontSize: 14,
    color: '#666',
    marginBottom: 8,
  },
  linkDisabled: {
    opacity: 0.5,
  },
  // ... existing code ...
}); 